package movieservice.enums;

/**
 * Configured demand level for a cinema cluster. The numeric demandScore remains
 * the allocation input; this tier is the human-readable operational grouping.
 */
public enum DemandTier {
    HIGH,
    NORMAL,
    LOW
}
